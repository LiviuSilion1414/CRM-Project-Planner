<RadzenRow class="row mt-3">
    <RadzenColumn class="col-lg-3 col-md-5">
        <RadzenText TextStyle="TextStyle.Body1" class="fw-semibold" Text="Full name" />
    </RadzenColumn>
    <RadzenColumn class="col-lg-9 col-md-7 w-100">
        <RadzenText>@SelectedEmployee.fullname</RadzenText>
    </RadzenColumn>
</RadzenRow>

<RadzenRow class="row mt-3">
    <RadzenColumn class="col-lg-3 col-md-5">
        <RadzenText TextStyle="TextStyle.Body1" class="fw-semibold" Text="Username/Email" />
    </RadzenColumn>
    <RadzenColumn class="col-lg-9 col-md-7 w-100">
        <RadzenText>@SelectedEmployee.username</RadzenText>
    </RadzenColumn>
</RadzenRow>

<RadzenRow class="row mt-3">
    <RadzenColumn class="col-lg-3 col-md-5">
        <RadzenText TextStyle="TextStyle.Body1" class="fw-semibold" Text="Roles" />
    </RadzenColumn>
    <RadzenColumn class="col-lg-9 col-md-7 w-100">
        <RadzenText>@SelectedEmployee.employeesRolesCommaSeparatedString</RadzenText>
    </RadzenColumn>
</RadzenRow>

<RadzenRow class="row mt-3">
    <RadzenColumn class="col-lg-3 col-md-5">
        <RadzenText TextStyle="TextStyle.Body1" class="fw-semibold" Text="Created at" />
    </RadzenColumn>
    <RadzenColumn class="col-lg-9 col-md-7 w-100">
        <RadzenText>@SelectedEmployee.creationDateString</RadzenText>
    </RadzenColumn>
</RadzenRow>

<RadzenRow class="row mt-3">
    <RadzenColumn class="col-lg-3 col-md-5">
        <RadzenText TextStyle="TextStyle.Body1" class="fw-semibold" Text="Last seen at" />
    </RadzenColumn>
    <RadzenColumn class="col-lg-9 col-md-7 w-100">
        <RadzenText>@SelectedEmployee.lastSeenString</RadzenText>
    </RadzenColumn>
</RadzenRow>

<RadzenRow AlignItems="AlignItems.Center" class="row mt-3">
    <RadzenColumn>
        <RadzenButton ButtonType="ButtonType.Button" ButtonStyle="ButtonStyle.Danger" Variant="Variant.Outlined" Text="Delete" Click="OnDelete"></RadzenButton>
        <RadzenButton ButtonType="ButtonType.Button" ButtonStyle="ButtonStyle.Primary" Variant="Variant.Outlined" Text="Edit" Click="OnEdit"></RadzenButton>
    </RadzenColumn>
</RadzenRow>

@code
{
    [Parameter] public EmployeeDto SelectedEmployee { get; set; }
    [Parameter] public EventCallback<EmployeeDto> OnEditCallback { get; set; }
    [Parameter] public EventCallback<EmployeeDto> OnDeleteCallback { get; set; }
    [Parameter] public EventCallback OnReloadList { get; set; }

    private async Task OnEdit()
    {
        await OnEditCallback.InvokeAsync(SelectedEmployee);
    }

    private async Task LoadData()
    {
        try
        {
            fetch.IsBusy = true;

            var filter = new EmployeeFilterDto() { employeeId = SelectedEmployee.id };

            var result = await execute.Employee_Get(filter);

            if (result.data is not null && result.hasCompleted && result.messageType == MessageType.Success)
            {
                SelectedEmployee = JsonSerializer.Deserialize<EmployeeDto>(result.data.ToString());
            }
            fetch.IsBusy = false;
        }
        catch
        {
            notification.Notify(NotificationSeverity.Warning, "Something went wrong, please retry", null, 4000);
        }
    }

    private async Task OnDelete()
    {
        await OnDeleteCallback.InvokeAsync(SelectedEmployee);
    }

    async Task InvokeReloadList()
    {
        await OnReloadList.InvokeAsync();
        await LoadData();
    }
}
