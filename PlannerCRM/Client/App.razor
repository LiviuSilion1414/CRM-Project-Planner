<head>
    @*

    <RadzenTheme Theme="humanistic" @rendermode="RenderMode.InteractiveServer" />
    <RadzenTheme Theme="software" @rendermode="RenderMode.InteractiveServer" />
    <RadzenTheme Theme="material" @rendermode="RenderMode.InteractiveServer" />

    *@
    <RadzenTheme Theme="standard" @rendermode="RenderMode.InteractiveServer" />


</head>

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Not found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <NotFoundPage />
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>