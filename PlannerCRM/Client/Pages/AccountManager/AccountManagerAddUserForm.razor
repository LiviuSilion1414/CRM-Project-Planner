@page "/account-manager/add/user"

@using PlannerCRM.Shared.DTOs.EmployeeDto.Forms
@using PlannerCRM.Shared.Models

@inject NavigationManager navManager
@inject HttpClient httpClient

@attribute [Authorize(Roles = nameof(Roles.ACCOUNT_MANAGER))]

<PageTitle>Account Manager</PageTitle>

<div class="container-fluid">
    <h1>Aggiungi utente</h1>
    <EditForm Model="@_model" OnValidSubmit="OnClickConfirm" class="form">
        <DataAnnotationsValidator/>
        <div class="form-group">
            <label for="Nome">Nome</label>
            <input type="text" class="form-control" @bind="_model.FirstName" required/>
            <ValidationMessage For="@(() => _model.FirstName)"/>
        </div>
        <div class="form-group">
            <label for="Cognome">Cognome</label>
            <input type="text" class="form-control" @bind="_model.LastName" required/>
            <ValidationMessage For="@(() => _model.LastName)"/>
        </div>
        <div class="form-group">
            <label for="Email">Email</label>
            <input type="email" class="form-control" @bind="_model.Email" required/>
            <ValidationMessage For="@(() => _model.Email)" />
        </div>
        <div class="form-group">
            <label for="Data di nascita">Data di nascita</label>
            <input type="date" class="form-control" @bind="@_model.BirthDay" required/>
            <ValidationMessage For="@(() => _model.BirthDay)" />
        </div>
        <div class="form-group">
            <label for="Data d'inizio">Data d'inizio</label>
            <input type="date" class="form-control" @bind="@_model.StartDate" required/>
            <ValidationMessage For="@(() => _model.StartDate)" />
        </div>
        <div class="form-group">
            <label for="Codice fiscale">Codice fiscale</label>
            <input type="text" class="form-control" @bind="_model.NumericCode" required/>
            <ValidationMessage For="@(() => _model.NumericCode)" />
        </div>
        <div class="form-group">
            <label for="Ruolo">Ruolo
                <div class="form-control">
                    <select class="dropdown-item" @bind="_model.Role" required>
                        @foreach (var role in Enum.GetValues(typeof(Roles))) {
                            if ((Roles)role != Roles.ACCOUNT_MANAGER) {
                                <option value="@role">@role</option>
                            }
                        }
                    </select>
                    <ValidationMessage For="@(() => _model.Role)" />
                </div>
            </label>
        </div>
        <div class="form-group">
            <label for="Tariffa oraria">Tariffa oraria</label>
            <input type="number" class="form-control" @bind="_model.HourPay" required/>
            <ValidationMessage For="@(() => _model.HourPay)" />
        </div>
        <div class="form-group">
            <label for="Password">Password</label>
            <input type="password" class="form-control" @bind="_model.Password" required/>
            <ValidationMessage For="@(() => _model.Password)" />
        </div>
        <div class="wrapper-btn">
            <div class="btn-cancel">
                <button type="button" class="btn btn-link" @onclick="OnClickCancel">Annulla</button>
            </div>
            <div class="btn-confirm">
                <button type="submit" class="btn btn-primary">Conferma</button>
            </div>
        </div>
    </EditForm>
</div>

@code {
    private EmployeeForm _model = new();

    private void OnClickCancel() {
        navManager.NavigateTo("/account-manager");
    }

    private async void OnClickConfirm() {
        await httpClient.PostAsJsonAsync<EmployeeForm>("http://localhost:5032/account/add/user", _model);
        await httpClient.PostAsJsonAsync<EmployeeForm>("http://localhost:5032/employee/add", _model);

        navManager.NavigateTo("/account-manager");
    }
}