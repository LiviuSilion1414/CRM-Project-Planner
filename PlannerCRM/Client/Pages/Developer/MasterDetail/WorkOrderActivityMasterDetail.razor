@if (_activities is null) {
    <p><em>Non ti è stata ancora assegnata alcuna attività.</em></p>
} else {
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Commessa</th>
                <th>Attività</th>
                <th>Data d'inizio</th>
                <th>Data di fine</th>
                <th>Ore segnate</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ac in _activities) {
                <tr>
                    <td><span class="oi oi-briefcase"></span></td>
                    <td>
                        @(_workorders
                            .Distinct()
                            .FirstOrDefault(wo => wo.Id == ac.WorkOrderId)
                            .Name)
                    </td>
                    <td>@ac.Name</td>
                    <td>@ac.StartDate.ToShortDateString()</td>
                    <td>@ac.FinishDate.ToShortDateString()</td>
                    @if (_workTimeRecords is null) {
                        <td><em>Nessun orario.</em></td>
                    } else {
                        <td>
                            @foreach (var wtr in _workTimeRecords.Distinct()) {
                                if (wtr.ActivityId == ac.Id) {
                                    @wtr.Hours
                                }
                            }
                        </td>
                    }
                    <td>
                        <button type="button" class="btn btn-primary" 
                            @onclick="@(() => OnClickAddWorkedHours(ac.Id))">
                            <span class="oi oi-clock"></span>
                            <span>Aggiungi orario</span>
                        </button> 
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@if (_IsAddClicked) {
    <DeveloperAddWorkedHours ActivityId="@_ActivityId" EmployeeId="@EmployeeId"/>
}